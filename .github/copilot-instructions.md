Follow `AGENTS.md` as the repository-wide self-dogfood operating contract.

Control plane: https://github.com/Dylan5237/agent-project-ops/issues/10

Read local `PRINCIPLES.md` before proposing repository-state changes. GitHub `origin` is the sole write authority; projection is `(none)` unless the Command Center changes it. Never push directly to `main`, never self-issue disposer decisions, and do not recursively vendor this methodology repository into itself.
