---
name: agent-project-ops
alwaysApply: true
---

Obey the repository root `AGENTS.md` and `.agent-project-ops/PRINCIPLES.md`. One GitHub write authority (`origin`). Optional `projection` remote is mirror/fast-forward only. Chat is not state. Do not force the default branch. Work in `.worktrees/`.
